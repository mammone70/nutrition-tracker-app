# REPORTING
SENTRY_DNS="DNS_URL"

# Supabase Backend (food search catalog — not diary sync)
SUPABASE_PROJECT_URL="PROJECT_URL"
SUPABASE_PROJECT_ANON_KEY="ANON_KEY"

# Calorie-tracker diary sync is configured in-app under
# Settings → Data → Calorie Tracker sync (base URL + bearer token in
# secure storage). See docs/calorie-tracker-sync.md.
